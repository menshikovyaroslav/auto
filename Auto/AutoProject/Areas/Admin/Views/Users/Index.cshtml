@using Front.Areas.Admin.Models;

@model IEnumerable<User>

@{
	ViewBag.Title = "User administration";
}

<h2>Users</h2>
<p>
	<a class="createlabel" asp-action="Create">Create new user</a>
</p>
<table>
	<tr>
		<th>UserName</th>
		<th>Email</th>
		<th>Change</th>
		<th>Roles</th>
		<th>Delete</th>
	</tr>
	@foreach (var item in Model)
	{
		<tr>
			<td>@item.UserName</td>
			<td>@item.Email</td>
			<td>
				<a class="editlabel" asp-action="Edit" asp-controller="users" asp-route-id="@item.Id">Изменить</a>
			</td>
			<td>
				<a class="editlabel" asp-action="Edit" asp-controller="roles" asp-route-id="@item.Id">Изменить</a>
			</td>
			<td>
				<form asp-action="Delete" method="post" asp-route-id="@item.Id">
					<input type="submit" value="Удалить"/>
				</form>
			</td>
		</tr>
	}
</table>